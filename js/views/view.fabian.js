"use strict";
import Bash_Route from "./../bash/bash-spa-route.js";

export default class FabianView extends Bash_Route{
    constructor(slug, template) {
        super(slug, template);
    }

    
}